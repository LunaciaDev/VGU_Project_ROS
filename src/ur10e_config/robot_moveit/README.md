# robot_moveit

This package provides the MoveIt! configuration for the UR10e robot mounted with the RG2 gripper.

# Overview

MoveIt Setup Assistant can be used as a GUI editor for this package.

`config` holds the configuration for various aspect of the robot for MoveIt and ROS.
`launch` holds the launch file to bring up various components needed for MoveIt.