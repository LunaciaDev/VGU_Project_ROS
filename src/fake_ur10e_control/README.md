# fake_ur10e_control

This package provide a simulated hardware interface for the UR10e, which execute goal and trajectory commands sent by MoveIt via linear interpolation and publish the simulated joint state back for both MoveIt and Unity.

This package is built based on [`ros_control_boilerplate`](https://github.com/PickNikRobotics/ros_control_boilerplate).